{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Typechecks the tests, which the app config deliberately excludes: `tsc --noEmit` there is scoped to what Next builds. Without this, test files are only ever transpiled by vitest, which strips types without checking them — so a test could construct a row the schema would reject and still pass.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "incremental": false,
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "test/**/*.ts", "vitest.config.ts"]
}
